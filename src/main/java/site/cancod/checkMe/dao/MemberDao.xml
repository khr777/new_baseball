<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="site.cancod.semi_shop.dao.MemberDao">

	<insert id="insertJoin">
        INSERT INTO Member
        SET loginId = #{loginId},
        loginPw = #{loginPw},
        name = #{name},
        email = #{email},
        create_time = NOW()
    </insert>
    
    <select  id="getCheckLoginId" resultType="int">
    	SELECT COUNT(*)
    	FROM Member
    	WHERE loginId = #{loginId}
    </select>
    
    <select  id="getMemberByLoginId" resultType="Member">
    	SELECT *
    	FROM Member
    	WHERE loginId = #{loginId}
    </select>
	
</mapper>